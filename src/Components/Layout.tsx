import SideBar from './SideBar/SideBar';
import Header from './Header/Header';

const Layout = () => {
  return (
    <div>
      <SideBar />
      <Header />
    </div>
  );
};

export default Layout;
